apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: cleanup-and-test
spec:
  steps:
    - name: cleanup
      image: alpine
      command: ["echo"]
      args: ["Cleaning up..."]
    - name: test
      image: python:3.9-slim
      command: ["pytest"]
